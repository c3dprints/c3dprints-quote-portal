# C3D Prints Quote Portal v3

V3 simplifies the internal calculator.

Replace backend files and add `frontend/internal-quote-calculator.html`.

Render env should include:

DEFAULT_KWH=0.33
DEFAULT_MARKUP=50
